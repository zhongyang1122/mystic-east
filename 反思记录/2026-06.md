## [2026-06-19] yourbazi 部署巡检
- 结果：确认仓库和 origin/main 一致，线上内容已是最新，但 https 自定义域名证书未覆盖 `yourbazi.xyz`，已写排查报告
- 卡点：自动化 memory 路径环境变量未注入 shell，需要改用明确的 `~/.codex/automations/...` 路径
- 规则：自动化任务遇到 `$CODEX_HOME` 为空时，先回退检查 `/Users/zhongyangyy/.codex/automations/`
- 下次：先同时验证 repo 提交、CNAME、HTTP 响应和 TLS SAN，能更快区分“内容未发布”和“证书/Pages 配置异常”

## [2026-06-19] yourbazi 部署巡检复查
- 结果：再次确认 `origin/main`、HTTP 页面内容、首页 trust fix 标记和 canonical 都已上线，失败点仍是 `https://yourbazi.xyz` 证书只返回 `*.github.io`
- 卡点：GitHub Pages 仪表盘设置不在仓库内，无法从本地直接修复证书绑定
- 规则：部署巡检结论必须以可正常访问的 `https` 用户入口为准，`http` 命中新内容不能算发布成功
- 下次：优先抓 `openssl s_client` 的 subject/SAN 和有效期，能更快判断是否只是 Pages 证书未重签发

## [2026-06-19] yourbazi 部署巡检三查
- 结果：确认最新仓库提交 `f455125` 已对应到 HTTP 页面，首页仍可见 `Private`、`No account`、`English-first` 和 `https://yourbazi.xyz/` canonical，但 HTTPS 仍因证书主机名不匹配失败
- 卡点：仓库内没有可进一步触发 GitHub Pages 证书重签的安全改动点，问题停留在 Pages 自定义域名配置层
- 规则：同类巡检若仓库、CNAME、HTTP 内容都正常，就不要伪造“重新部署成功”，必须明确区分“内容已上线”和“HTTPS 入口仍失败”
- 下次：先读上次 deployment report 和反思，再补最新时间戳与 TLS 证据，避免重复摸排同一结论

## [2026-06-19] yourbazi 部署巡检四查
- 结果：再次确认 `origin/main`、HTTP 页面内容、A 记录和 `www` CNAME 都正常，线上最新内容已在 HTTP 可见，但 HTTPS 仍返回默认 `*.github.io` 证书
- 卡点：当前剩余问题不在仓库文件，而在 GitHub Pages 自定义域名证书未为 `yourbazi.xyz` 重签
- 规则：部署巡检报告里要同时写清 `HTTP 已命中新内容` 和 `HTTPS 仍不可用`，避免把内容发布状态和用户可访问状态混为一谈
- 下次：优先复用已有 report，只更新时间、提交号、DNS/TLS 证据，减少重复编辑

## [2026-06-21] yourbazi SEO 文章发布
- 结果：新增一篇围绕 “no fire in bazi career visibility” 的英文文章，补齐示例、表格、反思提示、免责声明和计算器 CTA，并更新 `insights` 列表入口
- 卡点：站内现有 `insights` 还只有草稿卡片，选题去重更多要依赖主站文案和 git 历史而不是现成文章库
- 规则：SEO 自动化写作前先检查 `insights/` 与最近提交，优先做“具体问题 + 具体场景”题，而不是泛定义页
- 下次：先从元素失衡、出生时辰、职业/关系等高意图交叉点挑题，会比 MBTI/基础介绍类更容易做出差异化
